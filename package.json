{
  "name": "okfn-chapter",
  "private": true,
  "description": "OK Network Chapter WordPress block theme — local wp-env helpers",
  "scripts": {
    "start": "wp-env start && wp-env run cli wp theme activate wordpress-theme",
    "stop": "wp-env stop",
    "destroy": "wp-env destroy",
    "wp": "wp-env run cli wp",
    "build:zip": "git archive --format=zip --prefix=okfn-chapter/ -o okfn-chapter.zip HEAD",
    "lint:json": "python3 -c \"import json, glob; [json.load(open(f)) for f in ['theme.json'] + glob.glob('styles/*.json')]\"",
    "lint:php": "find -L . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n 1 php -l"
  },
  "devDependencies": {
    "@wordpress/env": "^10.33.0"
  }
}